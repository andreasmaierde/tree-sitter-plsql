SELECT abcd,
       cdejfsdf,
       sdfkjslkdfj,
       sysdate as abcd,
       sysdate(a,b),
       (select abcd from abcd) as abcd
FROM   dsv_abcd inner join dsv_abcd ON (abcd = sxxf)
WHERE  abcd_id = 15;


